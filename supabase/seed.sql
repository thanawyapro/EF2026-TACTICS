-- seed.sql
-- Seed file for local development of EF26 Tactics Labs

-- Note: Since profiles depend on users created within auth.users (owned by Supabase Auth engine),
-- the client normally provisions initial default templates automatically.
-- This file is kept as a structured placeholder in case manual seed commands or roles are deployed.

SELECT 'Database schema instantiated successfully. System ready for Auth registration.' AS status;
